# ===========================================
# Database Configuration
# ===========================================

# Local development: Uses SQLite automatically (no config needed)

# Production (Turso): Get credentials from https://turso.tech
# TURSO_DATABASE_URL=libsql://your-database.turso.io
# TURSO_AUTH_TOKEN=your-auth-token

# ===========================================
# AI Provider Configuration (choose one)
# ===========================================

# Option 1: Vertex AI (recommended for high volume - better rate limits)
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# For local development: Use service account file
# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# For Vercel/production: Paste entire service account JSON as a string
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key":"..."}

# Option 2: Gemini API Key (simpler setup, lower rate limits)
# Get your key from: https://aistudio.google.com/apikey
# GEMINI_API_KEY=your-api-key-here

# Priority: Vertex AI is used if GOOGLE_CLOUD_PROJECT is set,
# otherwise falls back to GEMINI_API_KEY
