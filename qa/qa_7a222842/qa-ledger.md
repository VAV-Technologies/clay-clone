# DataFlow QA Master Ledger — run qa_7a222842

Source: Part-1 production-readiness fleet (capture-only). Sandbox fully torn down.

## Counts

- Total findings: **153** (incl. 4 pre-fleet)
- By result: PASS=103, FAIL=43, ERROR=3, BLOCKED=3, SKIPPED_NO_PROVIDER=1
- Issues (non-PASS) by severity: P0=2, P1=21, P2=21, P3=6
- By dimension: functional=82, integrity=44, security=7, perf=14, concurrency=6

## Paid cost

- A_auth_crud_security: 0 calls, ~$0
- B_enrichment_formula: 7 calls, ~$0.0025
- C_email_lookup: 8 calls, ~$0.04
- C1_concurrency_races: 10 calls, ~$0.0006
- C2_performance_scale: 0 calls, ~$0
- C4_data_integrity_e2e: 1 calls, ~$0.0003
- D_campaigns_agent_destructive: 3 calls, ~$0.02

## Issues (non-PASS), severity-sorted

| id | sev | dim | area | scenario | actual | root-cause hint |
|---|---|---|---|---|---|---|
| qa_7a222842-B_enrichment_formula-008 | P0 | integrity | POST /api/enrichment/setup-and-run (costLimitEnabled/maxCostPerRow) | FRAGILE cost cap: costLimitEnabled=true, maxCostPerRow=0.00001, 1 row | row completed successfully at metadata.totalCost=$0.0004122 — 41.2x over the $0.00001 cap; cell status=complete with full 3-sentence output; errorCount=0; no cap enforcement of any kind | costLimitEnabled/maxCostPerRow are persisted on the config but never read by the run/setup-and-run executor — no pre-call estimate gate and no post-call abort; uncapped real-money spend |
| qa_7a222842-C1_concurrency_races-001 | P0 | concurrency | enrichment | Fire TWO concurrent POST /api/enrichment/run for the SAME config+targetColumn+row (1 row, gpt-5-nano). Assert final cell coherent AND whether both jobs ran/charged (double-spend). | Both concurrent requests returned HTTP 200 success=true and EACH independently invoked the LLM and charged: Request A metadata={inputTokens:49,outputTokens:75,totalCost:0.0000349}; Request B metadata={inputTokens:49,outputTokens:203,totalCost:0.0000861}. Two distinct model calls billed for ONE logical cell. No claim guard. Final persisted cell is coherent (value=OK, status=complete, last-writer-wins = B's metadata; no partial-merge corruption) and column progress shows complete:1 — so integrity is OK but spend is doubled. | Run handler does SELECT row then a separate UPDATE with no row/cell-level lock or job-claim (no SELECT…FOR UPDATE / atomic compare-and-set on cell.status=processing), so two requests both pass the gate and both call the model — non-atomic claim → double-spend. |
| qa_7a222842-A_auth_crud_security-015 | P1 | security | DELETE /api/rows | DELETE /api/rows with NO tableId in body (only ids) | Deletes a row by id alone with no sheet scoping — a caller can delete any row id without specifying/owning the target sheet | DELETE handler keys off ids[] and ignores tableId; no check that ids belong to the stated/any authorized table |
| qa_7a222842-A_auth_crud_security-022 | P1 | security | POST /api/rows (foreign columnId in cell data) | SECURITY: write a row to Sheet2 using a columnId that belongs to Sheet1 (POST /api/rows) -> expect rejection, not silent cross-write | 201, silent orphaned-cell write. Cell is invisible in Sheet2 UI (no matching header), unreadable by Sheet2 filters/export — silent data corruption | Write path (POST/PATCH rows) does not validate cell-data keys against the target table's columns, unlike the read/filter/sort path |
| qa_7a222842-A_auth_crud_security-023 | P1 | security | PATCH /api/rows/{id} (foreign columnId) | SECURITY: PATCH a row using a foreign (Sheet1) columnId | 200, foreign cell key accepted and persisted (same gap as POST) | PATCH row handler shares the unvalidated write path |
| qa_7a222842-A_auth_crud_security-026 | P1 | integrity | POST /api/import/csv vs POST /api/rows (createdAt persistence) | Data-movement: persisted createdAt/updatedAt on CSV-imported rows vs POST /api/rows rows (single-shot, no retry) | CSV import writes a corrupted far-future createdAt (year 58404) and empty updatedAt; breaks any time-based sort/filter/audit on imported rows | import/csv likely stores Date.now() milliseconds into a column read as epoch-seconds (or new Date(ms) treated as new Date(seconds)) — ms->s scale error inflates the year |
| qa_7a222842-B_enrichment_formula-009 | P1 | perf | POST /api/formula/run | FRAGILE formula eval timeout: infinite-loop formula on 1 row, 30s client timeout, ONE attempt | formula '{{Domain}} + (function(){while(true){}return 1})()' caused the request to hang for the full 30s client timeout with NO response (operation timed out). No server-side eval timeout. Single controlled attempt only. | Formula evaluator runs untrusted JS synchronously (e.g. new Function / eval) on the request thread with no VM timeout/worker isolation; an infinite loop blocks the Node event loop indefinitely |
| qa_7a222842-B_enrichment_formula-010 | P1 | concurrency | GET /api/columns, GET /api/enrichment, GET /api/rows (read-only) | Worker-pool starvation downstream of unguarded eval / long-blocking batch calls | Same endpoint, same input, non-deterministic: GET /api/columns returned 3x in <1.5s then hung >25s; GET /api/enrichment hung >25s 3x then returned in 1.5s; GET /api/rows fine one moment, hung the next. Began right after the infinite-loop formula + blocking batch submits. | Blocked workers (pinned by the infinite-loop eval and/or 120s-blocking Azure batch submits) starve the pool; requests routed to a stuck worker hang, others succeed — whole-deployment availability blast radius from the missing eval timeout |
| qa_7a222842-B_enrichment_formula-017 | P1 | perf | POST /api/enrichment/batch | Batch enrichment submit on existing config (1-3 rows) | Request blocks ~120s then 502 ROUTER_EXTERNAL_TARGET_ERROR (Vercel router external-target timeout). Tried twice, both 120s/502. No jobId returned; target cells never flipped to batch_submitted (stayed empty), so no batch job was persisted. Batch CANCEL and batch partial-failure probe could not be exercised because no job ever existed. | Azure Batch submit call blocks synchronously inside the request handler and exceeds the ~120s Vercel->ACA router limit; should enqueue async and return jobId immediately |
| qa_7a222842-B_enrichment_formula-018 | P1 | perf | GET /api/enrichment/batch/status, GET /api/admin/batch-status | Batch status endpoints (by jobId / by tableId) | Both hang past 60s client timeout with no response (no status code). Status shape could not be asserted. Consistent with the same external-target/blocking issue plus worker starvation. | status handler makes a blocking Azure call (or shares the starved worker pool) instead of reading persisted job state quickly |
| qa_7a222842-B_enrichment_formula-022 | P1 | perf | POST /api/enrichment/setup-and-run (webSearchEnabled=true) | Web-search enrichment (PAID, 1 row, webSearchEnabled=true): assert webSearchCalls bounded <=3 | Request blocked ~120s then 502 ROUTER_EXTERNAL_TARGET_ERROR — the web-search path (Spider + 90s per-row + gpt-5-nano on separate Azure resource) exceeds the ~120s Vercel router limit, so it is unreachable synchronously via this URL. webSearchCalls could not be asserted. Not a provider-missing case (no '...not configured' error) — a gateway timeout. | web-search real-time enrichment can run up to 90s/row but the Vercel->ACA router caps at ~120s; combined latency trips the gateway before the bounded result can return |
| qa_7a222842-C_email_lookup-002 | P1 | integrity | POST /api/lookup/run | FRAGILE lookup dup-key collision (bug #5): two Companies rows share domain dupe.com with different Industry; People row dupe.com run through lookup | Dupe Tester (Domain=dupe.com) silently matched ONLY the SECOND source row: enrichmentData={"Company Name":"Dupe Beta","Domain":"dupe.com","Industry":"IndustryBeta","Size":"222"}. The first source row (Dupe Alpha / IndustryAlpha / 111) is silently dropped — last-write-wins hash-map collision, no warning, no indication a second match existed. | Lookup builds an in-memory map keyed by match-column value; duplicate keys overwrite so only the last source row survives. No de-dup detection or first-match guarantee. |
| qa_7a222842-C_email_lookup-003 | P1 | integrity | POST /api/lookup/run | lookup/run with FOREIGN inputColumnId (a Companies-sheet column id used as the People input column) | Returned 200 {processedCount:6, matchedCount:0, unmatchedCount:6, sourceRowCount:5}. The foreign inputColumnId was NOT rejected; the run silently matched nothing AND OVERWROTE every previously-matched cell with __no_match — e.g. Dupe Tester (previously matched) became enrichmentData={"__no_match":"no source row matched"}, status=complete. Silent destruction of good lookup results. | /api/lookup/run does not validate that inputColumnId/targetColumnId belong to tableId (no invalidColumnIds guard like /api/rows). Foreign id resolves to empty values for every row, then the run writes __no_match over existing cells. |
| qa_7a222842-C_email_lookup-017 | P1 | integrity | POST /api/find-email/run | find-email/run FOREIGN resultColumnId (a Companies-sheet column id passed as resultColumnId on the People sheet) | Returned 200 and INVOKED the provider, then wrote an orphan cell into the People row (Marc) keyed under the foreign Companies column id: People-Marc.data[Companies.Industry colId] = {value:null, status:error, error:'...404 Application not found', enrichmentData:{provider:ninjer}}. The People sheet has no such column, so the result is stranded/inaccessible. Had Ninjer succeeded, a real email would have been written to a phantom cell. (Companies sheet Industry values themselves stayed intact — Software/Travel/IndustryAlpha/IndustryBeta — the pollution is in the People row's JSON blob.) | find-email/run does not validate resultColumnId belongs to tableId (no invalidColumnIds guard). It blindly merges the result cell into the row data under the supplied column id, creating a cross-sheet orphan cell / silent data stranding. |
| qa_7a222842-C1_concurrency_races-002 | P1 | concurrency | campaigns | POST /api/campaigns with the SAME tiny plan TWICE within a few hundred ms (concurrent background curls). Record whether two independent campaigns + two top-level workbooks are created (double-create). | Both POSTs returned HTTP 201 with DIFFERENT ids and DIFFERENT workbooks: camp_d16cb809-f0b4-4751-82c4-ce7feb954385 (wb 69b446f0-9381-49da-8163-72ff9812a47e) and camp_12fe344f-26d9-430d-8bf9-55f14de2db1a (wb b68e5425-1276-475b-b0d7-dea993c2f366). Both ran to status=complete and each workbook independently built a 'Probe' sheet with 2 rows. Full double-create of campaign + workbook + data. For a plan containing paid search/AI/email steps this is a double-spend (P0-class). | No idempotency key or dedup on (name+steps hash) at campaign create; create_workbook step always inserts a fresh top-level workbook. Concurrent identical requests are treated as fully independent. |
| qa_7a222842-C1_concurrency_races-005 | P1 | concurrency | find-email | Webhook double-fire (AI Ark): provider availability check + fire two concurrent POST /api/find-email/ai-ark submits for the SAME row+resultColumn (two trackIds → two webhooks racing into one cell). Assert final cell coherent. | Both concurrent submits returned HTTP 200 status=submitted, submittedCount=1 each — i.e. TWO independent AI Ark submissions (2 API calls each = 4 calls) for one logical cell, no claim guard (same double-spend class as enrichment/run). Final cell DID resolve coherently after webhooks: status=complete, value=null, enrichmentData.status='not_found (done)', single provider=ai_ark + single track_id. So integrity is OK (last-writer-wins on the cell) but AI Ark credits are double-charged. The HMAC token is identical for the same tableId:rowId:resultColId, so both webhook callbacks are valid and target the same coordinate. | Same non-atomic claim issue as /enrichment/run — ai-ark submit path does not mark the target cell as claimed/processing before issuing AI Ark calls, so concurrent submits both proceed and both spend AI Ark credits; two webhooks with identical HMAC both write the same coordinate. |
| qa_7a222842-C1_concurrency_races-006 | P1 | integrity | enrichment | Validation/data-integrity: POST /api/enrichment/run with a valid configId but a targetColumnId that is NOT a column of the table (00000000-0000-0000-0000-000000000000). Should 400/404; assert it does not write/charge. | Returned HTTP 200 success=true, ran the LLM and charged $0.0000349, and WROTE the result into a phantom cell keyed by 00000000-0000-0000-0000-000000000000 in the row's data JSON. Re-fetch confirms the orphan key persists in row.data and is NOT present in GET /api/columns?tableId=… (REAL_COL_IDS excludes it). The write path does not validate that targetColumnId is a real column of the table — pollutes row data with an orphan cell AND bills a model call. | enrichment/run validates configId (404 on unknown) but not that targetColumnId is a column of tableId before merging the result into row.data — no membership check on the write path, unlike the read/filter path which now returns 400 invalidColumnIds. |
| qa_7a222842-C2_performance_scale-007 | P1 | integrity | GET /api/rows (limit=-1) | Pagination boundary: negative limit (limit=-1) at N=10000 | limit=-1 returns 9999 rows (N minus 1) instead of erroring or returning 0/all. This is raw Array.slice(0,-1) semantics leaking through — a client passing a bad/negative limit silently loses exactly the last row, a quiet data-truncation trap. | limit is passed unvalidated into Array.prototype.slice(offset, offset+limit); negative limit makes the end index negative => drops trailing rows. |
| qa_7a222842-C2_performance_scale-014 | P1 | perf | POST /api/formula/run (10,000 rows) | Run a formula over the large 10,000-row sheet and time it (docs claim <2s for 10k rows) | POST /api/formula/run on 10,000 rows ran synchronously inline (no jobId returned), exceeded the Vercel->ACA proxy 120s limit, and failed with ROUTER_EXTERNAL_TARGET_ERROR at 120178ms. Re-fetch confirms ZERO effect: the target 'Tier' column was NEVER created and NO rows were written (big sheet still has exactly 5 columns minutes later). Work was completely lost; no async fallback, no row cap. Directly contradicts API-REFERENCE 'Formula \| Instant \| 10,000 rows in <2 seconds'. | Formula endpoint evaluates all rows inline in the request lifecycle (loads whole table + computes), exceeding the edge proxy timeout; no background job, no chunking, no row limit. Same load-whole-table pattern as reads. |
| qa_7a222842-C2_performance_scale-015 | P1 | perf | Backend-wide (GET/POST /api/* during formula overload) | Blast radius of the oversized 10k formula request on unrelated endpoints | The single 10k synchronous formula request degraded the ENTIRE backend for several minutes. During/after it, unrelated calls failed with ROUTER_EXTERNAL_TARGET_ERROR: GET /api/columns, GET /api/stats, POST /api/tables, POST /api/columns, POST /api/rows on brand-new tiny sheets. Read path recovered first (~39s to first stats OK); write path lagged badly — POST /api/columns needed 4 retries (~24s) AFTER stats was already OK, and a fresh small formula/run retried 3x. A 3-consecutive-write stability gate only passed after ~4 minutes total. | Single-container ACA backend with no per-request isolation/concurrency cap; the runaway inline formula saturates CPU/event-loop (or the container restarts), starving all other requests. Writes recover slower than reads. |
| qa_7a222842-C4_data_integrity_e2e-011 | P1 | integrity | POST /api/export/csv | FRAGILE export fidelity (bug #6): POST /api/export/csv and inspect raw CSV for (a) {{...}} formula literal leak and (b) whether enrichment/hidden columns appear and with what content. | No {{...}} leak (formula col exports computed 'salesforce.com' -- bug #6 formula-literal leak does NOT reproduce). BUT enrichment-typed columns export their internal placeholder cell.value, NOT the enriched data: lookup column 'Company Info (Lookup)' exports the literal string 'matched' (4x) and blank for the unmatched row; AI column 'HQ Location (AI)' exports '5 datapoints' (the JSON-mode summary) for the enriched row and blank for the rest. The actual enrichmentData (San Francisco, United States, Software, Hardware, Annual Revenue, etc.) appears NOWHERE in the CSV. A user exporting an enriched sheet for CRM/outreach gets useless 'matched'/'5 datapoints' cells instead of the data they paid to enrich -- arguably worse than omitting the columns because it looks like data. | Export serializer writes cell.value verbatim for every column type; for enrichment columns cell.value is an internal status/summary token ('matched','N datapoints') rather than the meaningful field(s) in cell.enrichmentData. Export should special-case enrichment columns to emit enrichmentData (or the resolved return value), or flatten enrichmentData keys into columns. |
| qa_7a222842-D_campaigns_agent_destructive-004 | P1 | concurrency | GET /api/cron/process-campaigns (parallel) | Concurrency: fire 3 parallel cron ticks at the same running campaign/step (no-locking probe) | All 3 concurrent ticks targeted the SAME step 2 (import_csv): tick1='step 2 complete... advancing to 3', tick2='step 2 still running', tick3='step 2 complete... advancing to 3'. Two invocations both claimed to COMPLETE the same step — no locking. Data happened to stay correct (rowCount=3 not 6) because import_csv is idempotent, but a non-idempotent paid step (search_companies / find_emails_waterfall) would be double-executed and double-billed. | process-campaigns selects the current step without an atomic claim (no SELECT...FOR UPDATE / status compare-and-set), so overlapping invocations (scheduled cron + manual trigger, or retry storm) can both run the same step |
| qa_7a222842-Zpre-001 | P1 | functional | formula | formulajs "default" export leaks into new Function() params; every formula eval throws in pure ESM | Unexpected token 'default' (node/vitest). Prod verdict comes from group B formula tests. | spread ...formulajs includes reserved key "default"; filter evalContext to valid, non-reserved identifiers |
| qa_7a222842-A_auth_crud_security-004 | P2 | integrity | POST /api/projects | Create project with invalid type:'banana' (enum should be folder\|workbook\|table) | 201 Created with type='banana' persisted | POST /api/projects validates presence of name+type but not that type is in the allowed enum |
| qa_7a222842-A_auth_crud_security-007 | P2 | integrity | POST /api/tables | Create table with missing projectId vs non-existent projectId | 500 Failed to create table — DB FK error leaked as 500 | tables route does not pre-validate projectId existence; FK constraint violation surfaces as unhandled 500 |
| qa_7a222842-A_auth_crud_security-009 | P2 | integrity | PATCH /api/columns/{id} | PATCH column order=0 on a non-first column (reorder) | Duplicate order=0 on two columns; display order becomes ambiguous/nondeterministic on the tie | PATCH writes the new order value without re-sequencing other columns in the table |
| qa_7a222842-A_auth_crud_security-010 | P2 | integrity | POST /api/columns | Create column with missing tableId vs non-existent tableId vs invalid type | 500 on bad tableId (FK leak); 201 accepts bogus column type | No FK pre-check for tableId; no enum check for column type |
| qa_7a222842-A_auth_crud_security-016 | P2 | integrity | DELETE /api/rows | DELETE /api/rows with a non-existent row id | Reports deletedCount:1 for a row that does not exist — count is fabricated/inaccurate | deletedCount echoes ids.length rather than the actual affected-row count from the delete |
| qa_7a222842-A_auth_crud_security-024 | P2 | integrity | POST /api/import/csv | FRAGILE CSV type inference (bug #9): import CSV whose FIRST data row has EMPTY cells in columns that are numeric/email in later rows | Inference samples only row 0; empty leading cell mistypes the whole column as text (bug #9 confirmed) | Type-inference reads data[0][col] only instead of scanning the first non-empty value |
| qa_7a222842-A_auth_crud_security-033 | P2 | perf | GET /api/rows (Vercel -> Azure proxy) | Reliability: latency/timeouts on core read endpoint under sequential load | Sporadic ~25s+ stalls requiring retries; correctness unaffected but a reliability risk for an internal rollout | Vercel->Azure Container Apps proxy cold starts or backend connection-pool exhaustion; intermittent, not tied to a specific operator |
| qa_7a222842-B_enrichment_formula-007 | P2 | functional | PATCH /api/enrichment/{id} | Enrichment config PATCH ignores the 'name' field (silent partial update) and 400s on name-only PATCH | PATCH {name} alone -> 400; PATCH {name,prompt,inputColumns} -> prompt updated to 'x2 {{Domain}}' but name stayed 'NameTest_ORIG' in both response and re-GET (name silently dropped). Formula PATCH by contrast updates name correctly. | PATCH handler whitelists prompt/model/temperature/etc. but omits 'name' from the updatable field set; validation also requires prompt present (rejects name-only body) |
| qa_7a222842-B_enrichment_formula-014 | P2 | functional | POST /api/formula/run | Formula error handling: missing formula, bad ref column, bad tableId | no formula -> 400 (correct); '{{Nonexistent Column}}' -> cell status=error value empty (graceful, correct); BUT bad tableId -> 500 (server error) instead of 400/404 | unknown tableId not validated before use; downstream null/DB access throws and surfaces as unhandled 500 rather than a 404 |
| qa_7a222842-B_enrichment_formula-019 | P2 | functional | DELETE /api/enrichment/batch/cancel?columnId=... | Batch CANCEL the submitted job | Could not test: batch submit 502'd and no job was ever created, so there was nothing to cancel. Blocked by the batch-submit failure above. | upstream batch submit failure blocks this path |
| qa_7a222842-B_enrichment_formula-020 | P2 | integrity | POST /api/enrichment/batch + GET status | Batch partial-failure: would an all-errors batch still report 'complete'? | Could not reach this probe — batch submit never produced a job (502) and status hangs, so terminal status semantics could not be observed. | blocked by batch submit + status being unreachable through this deployment |
| qa_7a222842-C_email_lookup-004 | P2 | integrity | POST /api/lookup/run | lookup/run with unknown/nonexistent sourceTableId | Returned 200 {processedCount:6, matchedCount:0, unmatchedCount:6, sourceRowCount:0}. An all-zeros / nonexistent source table is silently treated as an empty source and zeroes out the target column with no error. | No existence check on sourceTableId; a missing table yields zero source rows rather than an error. |
| qa_7a222842-C_email_lookup-009 | P2 | functional | POST /api/find-email/run (Ninjer) | find-email/run (Ninjer) on 3 rows incl. bogus; result column type=enrichment, provider tag, no fake email on bogus | Endpoint + result-column wiring work (type=enrichment, actionKind=find_email_ninjer, actionConfig persisted; response is well-formed {processedCount:3, foundCount:0, errorCount:3}). BUT every row errored upstream: 'API error 404: {"code":404,"message":"Application not found"}'. provider='ninjer' correctly tagged in enrichmentData. The Ninjer 'Application' (account/credential) is not configured/registered upstream, so no real lookups succeed. Recorded SKIPPED_NO_PROVIDER per cost rules (provider unreachable). Bogus row correctly got NO fake email (value empty). | NINJER app id / API credential not registered with the Ninjer service ('Application not found' 404) — env/account misconfiguration, not a code bug. Blocks the documented Ninjer happy path in prod. |
| qa_7a222842-C2_performance_scale-004 | P2 | perf | GET /api/rows (filters=contains) | GET /api/rows with contains filter at scale — verify filter runs in JS over the whole table (bug #8) | Contains filter on Company Name: 1388ms@1k -> 1814ms@5k -> 2827ms@10k. Latency scales with TOTAL rows scanned (X-Total), not the matched result size. At N=10000 it scans 10000 rows to return 1111 (504KB) in 2.8s. X-Filtered-Count is correct (111@1k, 111@5k, 1111@10k). | Backend loads entire table into memory then Array.filter() in JS (documented bug #8) — no DB-level predicate pushdown. |
| qa_7a222842-C2_performance_scale-005 | P2 | perf | GET /api/rows (limit=1) | GET /api/rows with limit=1 offset=0 at N=10000 — does pagination push the limit to the DB? | Fetching a SINGLE row (446-byte body) at N=10000 takes ~894ms; at N=5000 ~1143ms. A row-count-independent op (DB LIMIT 1) would be tens of ms. Latency tracks table size, proving the whole table is materialized before the JS slice. | Pagination is applied via Array.slice() AFTER loading all rows into memory; the DB query has no LIMIT/OFFSET. |
| qa_7a222842-C2_performance_scale-008 | P2 | integrity | GET /api/rows (offset=-5) | Pagination boundary: negative offset (offset=-5) at N=10000 | offset=-5 returns 0 rows (X-Total still 10000) instead of clamping to 0 and returning the first rows. A negative offset silently yields an empty page. | slice(-5, -5+10)=slice(-5,5) on a 10000-element array yields [] (start index after end); unvalidated negative offset. |
| qa_7a222842-C4_data_integrity_e2e-017 | P2 | functional | POST /api/export/csv (error inputs) | Error lens: POST /api/export/csv with missing tableId and with an unknown well-formed tableId. | missing tableId -> 400 (correct). Unknown tableId (00000000-...) -> request HANGS and times out at 25s with no response (vs GET /api/rows which returns 200 [] instantly for the same id). The export path handles a non-existent table by hanging instead of erroring -- a potential worker-tie-up / resource exhaustion vector under repeated bad input. | Export likely awaits a row stream/aggregation that never resolves (or a query that blocks) when the table doesn't exist; add an early table-existence check and a hard timeout. |
| qa_7a222842-C4_data_integrity_e2e-019 | P2 | perf | Vercel->Azure proxy for /api/* (global) | Infrastructure reliability: the Vercel front-door rewrite to the Azure backend intermittently returns ROUTER_EXTERNAL_TARGET_ERROR (gateway 5xx) on otherwise-valid requests. Quantified via burst probes. | Intermittent gateway failures across the whole proxied surface, NOT data corruption (every call that got through returned correct data; succeeds on retry). Burst probes: GET /api/columns = 4/8 failed, then 7/10 failed; GET /api/stats = 4/6 failed; GET /api/projects = 0/6 failed in the same window. Errors are Vercel edge errors 'ROUTER_EXTERNAL_TARGET_ERROR sin1::<id>' (Singapore edge) and one bulk-PATCH 502 -- consistent with a flapping/cold backend container behind the rewrite. For an internal rollout this produces frequent spurious failures and would require client-side retry. | Azure Container App backend behind the Vercel /api/* rewrite is scaling to zero / cold-starting or has unhealthy replicas, so bursts hit dead targets and the Vercel router returns ROUTER_EXTERNAL_TARGET_ERROR. Check ACA min-replicas/health probes and Vercel rewrite target health. |
| qa_7a222842-D_campaigns_agent_destructive-006 | P2 | functional | POST /api/campaigns | Error/invalid: POST /api/campaigns with an unknown step type (this_is_not_a_real_step) | Returned 2xx and created campaign camp_8e8880f6 with totalSteps=2 (create_workbook auto-prepended) AND auto-created a real top-level workbook. Only on the cron tick did it fail: status=error, step error='Unknown step type: this_is_not_a_real_step'. So a typo'd step type passes creation, wastefully provisions a workbook, and surfaces only at runtime. | No allowlist validation of step.type at POST time; validation deferred to the executor dispatch switch |
| qa_7a222842-Zpre-002 | P2 | functional | search | clay-api reads/writes estimatedTotal on a type that lacks it | TS2339/TS2353 property does not exist | return type missing estimatedTotal field |
| qa_7a222842-Zpre-003 | P2 | functional | enrichment/batch | status compared to 'request sent' which is never a valid value (dead branch) | TS2367 types have no overlap | stale status literal; dead code path |
| qa_7a222842-C1_concurrency_races-003 | P3 | integrity | campaigns | Re-parent each duplicate campaign-created top-level workbook under the sandbox/QA workbook (PATCH /api/projects/{id} {parentId}) so teardown can find them. | Sandbox folder id was provided as literal 'undefined' (no real folder id), so re-parenting under a sandbox folder was impossible. Attempting PATCH parentId=<my QA workbook id> returned HTTP 400 (workbooks cannot be nested under another workbook — only under folders). Both leaked workbook ids ARE recorded in workbookIdsCreated so teardown deletes them by id regardless of parent. | Sandbox folder id placeholder was unset ('undefined'); projects API correctly rejects workbook-under-workbook nesting. Not a product bug — environment/placeholder limitation. Recorded for teardown. |
| qa_7a222842-C2_performance_scale-012 | P3 | functional | POST /api/projects (depth guard 400) | Folder depth guard error quality — does the 400 explain the limit? | The depth-guard 400 returns a ZERO-LENGTH body (no error message, no limit value). Same for folder-under-workbook 400. A client hitting the limit gets 'Bad Request' with no machine- or human-readable reason — no {error:...} JSON. | Guard throws/returns NextResponse with status 400 but no JSON body, or the proxy strips the body on 400. |
| qa_7a222842-C4_data_integrity_e2e-016 | P3 | functional | GET /api/rows (error inputs) | Error lens: GET /api/rows with missing tableId, unknown well-formed tableId, and no Authorization header. | missing tableId -> 400 {error:'tableId is required'} (good). no auth -> 401 {error:'Authentication required...'} (good). BUT unknown well-formed tableId (00000000-0000-0000-0000-000000000000) -> 200 [] (empty array) instead of 404. A typo'd/stale tableId silently returns 'no rows' rather than signalling the table does not exist, which can mask bugs in caller pipelines. | Row query does not verify table existence before running the (empty) result query; add an existence check -> 404 when table missing. |
| qa_7a222842-D_campaigns_agent_destructive-003 | P3 | functional | GET /api/cron/process-campaigns | Cron stepping is inconsistent: sometimes completes multiple steps in one tick, and an eager synchronous advance makes a tick report 'No active campaigns' while the step it just completed shows complete | In one run a single tick reported 'completed (all 4 steps done)' advancing TWO pending filter steps at once; in another, tick2 returned {"message":"No active campaigns"} yet the filter_rows step had just transitioned to complete. The engine appears to advance some steps eagerly/synchronously rather than strictly one per tick. | Processor likely loops/chains trivial synchronous steps within a single invocation and reports state observed at scan time; messaging doesn't reflect the eager advance |
| qa_7a222842-D_campaigns_agent_destructive-024 | P3 | functional | POST/DELETE /api/campaigns, POST /api/agent/conversations, DELETE /api/projects (folderNotEmpty), PATCH /api/projects | Error responses across 4xx/409 paths carry no body (no error code or message) | Every 400/409 I triggered returned an EMPTY response body (ErrorDetails.Message blank): missing-steps 400, empty-steps 400, missing-name 400, double-cancel 400, cancel-complete 400, retry-complete/cancelled 400, empty-prompt 400, folderNotEmpty 409, reparent 400. Clients get only a status code with no machine-readable reason. | Handlers likely return new Response(null,{status}) / NextResponse with no JSON payload on these error branches; or the body is sent but not surfaced — either way no error code reaches the client |
| qa_7a222842-Zpre-004 | P3 | integrity | import | import/csv column insert omits actionKind/actionConfig (schema drift) | TS2345 missing actionKind/actionConfig | insert literal not updated when schema gained actionKind/actionConfig |

## Group summaries

### A_auth_crud_security
Exhaustively tested Group A (projects/folders, tables, columns, rows incl. server-side filter/sort/paginate, CSV import/export, stats, jobs/status, docs, and a per-endpoint security pass) against LIVE prod https://dataflow-pi.vercel.app inside a self-created sandbox workbook (now fully torn down, zero residue). Happy paths overwhelmingly PASS with re-fetch verification: project/folder CRUD + depth-limit(5) + 409 folderNotEmpty + 404-on-deleted, table/column/row CRUD, cell-merge on single+bulk PATCH, X-Total-Count increment/decrement, all 11 filter operators, AND/OR logic, sort asc/desc, limit/offset/past-end. The known P0 column-scope trap is CLOSED on BOTH filter and sort (foreign columnId -> 400 invalidColumnIds echoing the bad id + tableId). Security is solid: protected endpoints 401 unauth, bad token -> 401 'Invalid API key', public routes (/api/auth, /api/docs/download) 200 unauth. Found 9 real bugs. HEADLINE P1s: (1) CSV-imported rows persist a corrupted createdAt (year 58404) and empty updatedAt while POST /api/rows rows are correct -- isolated to the import insert path, confirmed in raw JSON; (2) the row WRITE path (POST and PATCH /api/rows) accepts foreign-sheet columnIds with NO validation -- asymmetric to GET which rejects them -- silently persisting orphaned, UI-invisible cells; (3) DELETE /api/rows with no tableId still deletes by id alone (missing scoping on a destructive op). Plus P2s: invalid project/column type accepted (201, no enum validation), bad projectId/tableId -> 500 instead of 400/404, column reorder creates duplicate order values, DELETE of a nonexistent id reports deletedCount:1, intermittent ~25s GET /api/rows timeouts through the Vercel->Azure proxy. CSV type-inference bug #9 CONFIRMED (empty first-row cell mistypes column as text; control proves inference works when row0 populated). Export bug #6 NOT reproduced -- formulas export their computed value, not the literal {{...}}, and enrichment hidden metadata does not leak. Zero paid external calls made.

### B_enrichment_formula
Tested AI enrichment (setup-and-run, run, retry-cell, extract-datapoint, CRUD, jobs, batch, web-search) and formulas (run/rerun/generate + CRUD) against the live prod deployment (https://dataflow-pi.vercel.app) inside an isolated QA workbook (8f43da79-230e-4e00-b3f1-6c6ef27624b7, now deleted). Core happy paths PASS and persist correctly: setup-and-run created a single enrichment column (no Data Guide auto-columns), populated enrichmentData (city=Redmond/country=United States) with metadata.totalCost>0; run-on-existing-config, retry-cell, and extract-datapoint (materialized a text column) all verified by re-fetch; formula run/rerun produced correct persisted values; formula CRUD fully works; validation/auth (400/404/401) is correct; negative web-search control returned webSearchCalls=0; batch+webSearchEnabled correctly 400s. THREE serious bugs confirmed: (1) cost-cap-dead [P0] — costLimitEnabled+maxCostPerRow=0.00001 did NOT prevent a $0.0004122 run (41x over cap); (3) formula-timeout [P1] — an infinite-loop formula hung with no server-side eval timeout, pinning a worker; and as a direct consequence, intermittent worker-pool starvation degraded the whole backend (same GET endpoint alternately returned in <1.5s or hung >25s). Batch enrichment is effectively unusable through this deployment: batch submit and batch/status both 502/hang at ~120s (Vercel router ROUTER_EXTERNAL_TARGET_ERROR), and web-search positive path 502'd at 120s. Also found enrichment PATCH silently ignores the name field (and 400s on name-only PATCH), and formula/run on a bad tableId returns 500 instead of 4xx.

### C_email_lookup
Tested find-email (Ninjer/TryKitt/AI-Ark + webhook + retry-cell) and lookup (run + retry-cell) against the live prod deployment (https://dataflow-pi.vercel.app) inside my own sandbox workbook QA__C_email_lookup__undefined (id d1c8549c-e916-47b1-b3b1-043431c0bd58) with a People sheet (4 real + 1 bogus + 1 empty-domain) and a Companies sheet (incl. two dupe.com rows with different Industry for the dup-key trap). Headline results: (1) Lookup happy path works and persists ALL source fields into enrichmentData; (2) CONFIRMED dup-key last-write-wins collision (bug #5) — domain dupe.com silently returns only Dupe Beta/IndustryBeta, Dupe Alpha is dropped; (3) MISSING foreign-column validation on BOTH lookup/run and find-email/run — a foreign columnId is accepted (200) instead of 400 invalidColumnIds, and on find-email it writes an orphan error cell into the target row under a foreign column id (silent cross-sheet pollution, P1); (4) lookup with an unknown sourceTableId returns 200 sourceRowCount:0 instead of erroring, and a foreign-input-column lookup run silently WIPED previously-matched cells; (5) AI-Ark async webhook round-trip fully works (Marc -> mbenioff@salesforce.com VALID); (6) TryKitt works (not_found, no sentinel leak in cell.value); (7) Ninjer is provider-misconfigured upstream (404 'Application not found' on every row) -> recorded SKIPPED_NO_PROVIDER; (8) email-sentinel-leakage bug #7 could NOT be reproduced on the TryKitt non-waterfall not_found path — cell.value stayed null (PASS); retry-cell works for both lookup and find-email; validation 400s and 401 unauth behave correctly. Paid provider attempts kept tiny (~6 billable, est <$0.05).

### C1_concurrency_races
Tested concurrency/race conditions against live prod (https://dataflow-pi.vercel.app) inside my own sandbox workbook 112c4f93-7c0b-48b2-a584-4db589abf897 (Seed sheet, 2 rows). HEADLINE: (1) Enrichment double-run is non-atomic — two simultaneous POST /api/enrichment/run for the SAME config+targetColumn+row BOTH ran the model and charged distinct costs (A=$0.0000349/75 out-tok, B=$0.0000861/203 out-tok); the final cell is coherent (last-writer-wins, value=OK, no metadata corruption) but it is a real double-spend with no job-claim guard (P0 uncapped duplicate spend). (2) Campaign launch is NOT idempotent — two identical concurrent POST /api/campaigns produced two independent campaigns AND two separate top-level workbooks (69b446f0…, b68e5425…), each fully built with a Probe sheet + 2 rows, both reaching status=complete (P1; would be P0 double-spend on a plan with paid search/AI/email steps). (3) AI Ark email-finder is configured and exhibits the same double-submit race — two concurrent /api/find-email/ai-ark submits for one row+resultcol both returned submitted (4 AI Ark API calls for one logical cell); the cell still resolved coherently to status=complete / not_found(done) with a single provider record. (4) Bonus: discovered a missing-validation/data-integrity bug — POST /api/enrichment/run accepts a targetColumnId that is NOT a column of the table (e.g. 00000000-…), runs+charges the LLM, and writes an orphan column key into the row's data JSON (confirmed by re-fetch; the id is not in GET /api/columns). Negative controls passed: unauth→401, bad configId→404, empty steps→400, missing name→400, concurrent cell PATCH is coherent last-writer-wins with siblings intact. Paid usage tiny (gpt-5-nano + AI Ark, ~10 paid units, est ~$0.0004 model spend + minor AI Ark credits).

### C2_performance_scale
Performance/scale testing of DataFlow prod (https://dataflow-pi.vercel.app) using pure CRUD/query load (no paid calls). Created sandbox workbook 5dd8d0e0-e60d-4549-8485-3c01bc327b68, bulk-seeded one sheet to 1,000 -> 5,000 -> 10,000 rows (each row 5 text cells) via 500-row chunked POST /api/rows. WRITE path scales linearly and cleanly: ~137-141 rows/sec at every size (each 500-row chunk ~3.4-3.7s, no degradation 1k->10k). READ path confirms the documented in-JS load-whole-table architecture (bug #8): GET /api/rows latency tracks TOTAL table size, not result size. Smoking gun: at N=10,000 a limit=1&offset=0 request returning a single 446-byte row still takes ~894ms, and a contains filter returning 504KB takes 2.8s while scanning all 10k rows (X-Total=10000, X-Filtered=1111). Full no-filter fetch climbs 2.45s(1k)->3.47s(5k)->5.5s(10k) for a 4.5MB body; sortBy similar. Pagination boundary bugs found: negative limit=-1 returns N-1 rows (JS slice(0,-1) leak) and negative offset=-5 silently returns 0 rows. Folder depth guard fires correctly at depth 6 (5 nesting levels allowed) but ALL 400 errors (depth guard, folder-under-workbook) return EMPTY bodies. THE HEADLINE FAILURE: POST /api/formula/run over 10,000 rows is synchronous/inline with no row cap and no async job — it blew past the Vercel->ACA proxy's 120s limit (ROUTER_EXTERNAL_TARGET_ERROR), wrote ZERO data, created NO output column, AND rendered the ENTIRE backend (all endpoints, all sheets) unavailable for several minutes; write-path recovery lagged read-path by ~25s+. Formula correctness itself verified PASS on a 3-row sheet (8000->Enterprise, 2000->Mid, 100->SMB). Reached sizes: 10,000 rows. All sandbox resources (workbook + 5-deep folder chain) torn down and verified gone.

### C4_data_integrity_e2e
Drove a full realistic pipeline (CSV import -> server-side filter -> cross-sheet lookup -> 1 paid AI enrichment on gpt-5-nano -> CSV export -> re-import round-trip) inside my own workbook QA__C4_data_integrity_e2e__run (00a4532a-714b-4e3c-99ff-ce8d7769a228, now deleted), asserting persisted state by re-GET after EVERY step. Core data movement is functionally correct: CSV import infers the right typed columns (email/url/number/date/text) and lands all 5 rows; server-side filters return correct X-Filtered-Count incl. numeric greater_than/between on string-stored numbers; lookup carries the FULL source row into enrichmentData per-row with a __no_match sentinel for misses; the paid AI enrichment ($0.00025) persists structured enrichmentData + metadata and does NOT auto-create leak columns; PATCH /api/rows/{id} updates one cell and merges (other columns preserved); plain-column round-trip is byte-faithful (0 value mismatches, types preserved). Headline BUG (export fidelity / bug #6): the CSV export does NOT leak {{...}} formula literals (formula column exports computed values), but enrichment-typed columns export internal PLACEHOLDER strings -- lookup cells export the literal 'matched' and JSON-mode AI cells export '5 datapoints' instead of the actual enriched values; the real enrichmentData (San Francisco, Software, etc.) never reaches the CSV, so it is permanently lost on export and on export->reimport round-trip. Secondary bugs: POST /api/export/csv with an unknown tableId HANGS/times out (>25s) instead of 400/404; and the Vercel->Azure front-door intermittently returns ROUTER_EXTERNAL_TARGET_ERROR (5xx) on otherwise-valid reads -- a burst probe saw 7/10 GET /api/columns and 4/6 GET /api/stats fail while GET /api/projects passed 6/6 in the same window (transient, recovers on retry, no data corruption). Minor: GET /api/rows with an unknown-but-well-formed tableId returns 200 [] rather than 404. 1 paid call total.

### D_campaigns_agent_destructive
Tested the campaigns engine, Agent X conversations, the campaign cron, and all destructive cascade/delete paths against live prod (https://dataflow-pi.vercel.app). Created my own sandbox workbook QA__D_campaigns_agent_destructive__undefined plus 6 campaign-spawned top-level workbooks; all torn down at the end (verified 404). Core happy paths, lifecycle transitions (pending->running->complete), cancel/delete, retry semantics, cascade-delete (no orphaned configs/jobs), nuke-table, and folder-not-empty (409) all PASS with re-fetch/data-movement verification. Two real defects found: (P1) the cron has NO concurrency lock — 3 parallel /api/cron/process-campaigns ticks all claimed to process the SAME step (import_csv ran "complete" twice in one race), which is safe only because import happened to be idempotent (row count stayed 3, not 6) but would double-bill paid steps (search/find_emails); and (P2) POST /api/campaigns accepts an unknown step type at creation (no schema validation) and only fails at runtime after wastefully auto-creating a workbook. Notable lesser issues: the stale-column-map case is handled gracefully (no crash) but deleting a column leaves orphaned cell data inside row.data keyed by the dead column id; nearly every 4xx/409 error response has an EMPTY body (no error code/message), hurting DX; cron stepping is inconsistent (sometimes 1 step/tick, sometimes multiple, plus an eager synchronous advance that makes a tick report "No active campaigns" while a step it just finished shows complete). Paid usage: 1 gpt-5-mini conversation turn + 1 AI-Ark preview (estimatedTotal 0, ~$0) + 1 gpt-5-nano setup-and-run ($0.00009). Approval "approve" turn correctly short-circuited the LLM (no charge).


## Teardown

Teardown complete; zero sandbox objects remain. NOTE: the orchestrator's template substitution failed, so runId, base URL, and the sandbox folder id all arrived as the literal string "undefined". Recovered real values: base URL https://dataflow-pi.vercel.app (from scripts/test_result_columns.ps1) and the actual sandbox root folder QA-SANDBOX__qa_7a222842__20260608T081843Z (id 0d732428-9801-46d0-ad68-f5aad7ced3e9), which was the SOLE remaining project and the unambiguous QA sandbox root for this run (today's timestamp 20260608, QA-SANDBOX__ prefix, empty). Step 1 (explicit workbook ids): 4 of 18 were live and deleted (d1c8549c, 112c4f93, 69b446f0, b68e5425); the other 14 returned HTTP 404 (already gone in prior runs) and were ignored per idempotency. Step 2: GET /api/projects returned only the QA-SANDBOX folder; its nested children[] and tables[] were both empty (all child workbooks already removed), and NO project name contained the literal substring "undefined". Deleted the empty sandbox root folder via DELETE /api/projects/0d732428-... (succeeded with no 409 because it was already empty). Step 3: re-GET /api/projects returned 0 projects; an explicit filter for names matching /undefined/ OR QA-SANDBOX* found nothing. No objects could not be deleted, so leaked[] is empty. No non-matching objects were touched.
folderDeleted=true, deleted=5, leaked=0
